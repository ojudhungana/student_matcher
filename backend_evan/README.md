# student_matcher

# install Node.js LTS

# create a .env in backend_evan folder (where .env.example is)
# copy+paste .env.example
# change the two lines (url and key) to what is pinned in discord

# command line
# cd into backend_evan folder

# may need:
# npm i multer
# npm i -D @types/multer


# npm install
# npm run build; npm start

# Open http://localhost:4000/health -> you should see {"ok":true}